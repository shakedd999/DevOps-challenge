apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.Name }}
spec:
  to:
    kind: Service
    name: {{ .Values.Name }}
  port:
    targetPort: {{ .Values.service.port }}
  tls:
    termination: edge
